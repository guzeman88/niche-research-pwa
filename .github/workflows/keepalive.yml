name: Backend Keepalive Disabled

on:
  workflow_dispatch:

jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled keepalive
        run: |
          echo "No live backend URL is configured for keepalive."
          echo "The PWA now builds from bundled real-data snapshots instead of pinging a suspended host."
