name: Backend Keepalive Disabled

on:
  workflow_dispatch:

jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled keepalive
        run: |
          echo "No live backend URL is configured for keepalive."
          echo "Production snapshots are built from Supabase. Backend startup is an explicit operator action."
